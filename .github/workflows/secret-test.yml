name: Test Environment Secrets

on:
  workflow_dispatch:  # manuel tetikleme
  
jobs:
  test-secrets:
    runs-on: ubuntu-latest
    environment: ltq   # <-- bunu kendi environment ismine göre değiştir

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show secret presence (without revealing)
        env:
          DB_USER_NAME: ${{ secrets.DB_USER_NAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: |
          echo "🔐 DB_USER_NAME loaded: $(echo -n "$DB_USER_NAME" | wc -c) chars"
          echo "🔐 DB_PASSWORD loaded: $(echo -n "$DB_PASSWORD" | wc -c) chars"
          echo "✅ Secrets have been successfully loaded!"
